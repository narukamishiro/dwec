function parimpar(){
	nm=0;
	base=new array();
	par=new array();
	impar=new array();
	for(x=0;x<1000;x++){
		nm=Math.floor(Math.random()+1000+1);
		base.push(nm);	
	}
	for each (var nm in base){
		if(nm%2==0){
			par.push(nm);
		}else{
			impar.push(nm);
		}
	}
	for(x=0;x<par)
}